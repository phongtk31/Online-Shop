spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onlineshop
    driver-class-name: org.postgresql.Driver
    username: admin
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: shopdb   # 👈 chỉ định schema mặc định
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
